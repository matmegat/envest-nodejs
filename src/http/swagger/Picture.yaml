type: object
title: Picture
description: Profile's image
properties:
  hash:
    type: string
    description: image hash (domain- and path- agnostic)
  path:
    type: string
    description: path to image (domain-agnostic)
  resolved_uri:
    type: string
    description: fully-qualified URI to image
required:
  - hash
  - path
  - resolved_uri
