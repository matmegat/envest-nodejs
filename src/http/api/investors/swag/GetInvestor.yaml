get:
  description: Get selected Investor
  parameters:
  - name: investor_id
    in: path
    type: integer
    description: Feed Item ID
    required: true
  responses:
    200:
      description: Investor
      schema:
        $ref: ./Investor.yaml
    400:
      description: if something went wrong
      schema:
        $ref: ../../../swagger/Error.yaml
  tags:
  - investors
